services:
  - type: web
    name: tecfag-mrp
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: tecfag-mrp-db
      mountPath: /opt/render/project/src/data
      sizeGB: 1
